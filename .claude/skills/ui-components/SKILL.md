# UI Components

Accessible, semantic and progressively enhanced.

Client branding is expected in client-owned components. Preserve stable
inherited `tly-*` primitive contracts and avoid a speculative widget library.
