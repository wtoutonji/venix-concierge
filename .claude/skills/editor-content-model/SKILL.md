# Editor / Content Model

Gutenberg is curated content editing.

Do not turn it into FSE site building.

Permanent project content models belong in client plugins, not the starter theme.

When a project curates blocks, preserve incoming `false`, intersect incoming
arrays, and preserve the WooCommerce Product policy by default.
