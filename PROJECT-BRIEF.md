# Project Brief

## Client
Venix Concierge

## Project type
standard

## Requirements
Document sitemap, content types, integrations, languages, editor expectations and special functionality here.

## Content model
Document which content belongs in Gutenberg, structured fields, CPTs,
taxonomies, Project Core, and Store Core when present.

## Design handoff
Approved Design System packages belong in `handoff/design-system/`; read their
README, completion report and migration map when present before mapping canonical
tokens/components to `theme.json`, WordPress presets, semantic aliases and shared
components. Approved self-contained `.dc.html` pages belong in `handoff/pages/`.
Supporting content, assets and references belong in their matching handoff directories.
